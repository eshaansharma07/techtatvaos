"use client";
import { useState } from "react";
import { ChevronDown, ChevronRight, Network, Users } from "lucide-react";
import { PublicShell } from "@/components/public-shell";
import { teams } from "@/lib/data";
export default function TeamsPage(){const [open,setOpen]=useState(1);return <PublicShell><section className="mx-auto max-w-6xl px-6 pb-28 pt-40"><p className="text-[10px] tracking-[.3em] text-violet-300">THE NETWORK</p><h1 className="mt-5 text-6xl font-medium tracking-[-.07em] md:text-8xl">Built to connect.</h1><p className="mt-6 max-w-2xl text-sm leading-7 text-white/45">An expandable view of the people and disciplines behind Tech Tatva.</p><div className="mt-14 rounded-2xl border border-white/[.08] bg-white/[.025] p-3 md:p-6"><div className="flex items-center gap-3 rounded-xl border border-violet-400/20 bg-violet-500/10 p-4"><Network size={18} className="text-violet-300"/><div><p className="text-sm">Tech Tatva</p><p className="mt-1 text-[10px] tracking-[.15em] text-white/35">CLUB OPERATING NETWORK</p></div></div><div className="ml-6 border-l border-violet-300/20 pl-5 pt-3">{teams.map(([name,copy,count],i)=><div key={name}><button onClick={()=>setOpen(open===i?-1:i)} className="my-2 flex w-full items-center gap-3 rounded-xl border border-white/[.06] bg-black/20 p-4 text-left transition hover:bg-white/[.05]">{open===i?<ChevronDown size={15}/>:<ChevronRight size={15}/>}<div className="flex-1"><p className="text-sm">{name}</p><p className="mt-1 text-xs text-white/35">{copy}</p></div><span className="text-xs text-violet-300">{count}</span></button>{open===i&&<div className="ml-6 grid gap-2 border-l border-white/10 py-2 pl-5 sm:grid-cols-3">{["Team Lead","Co-Lead","Core Members"].map((role,j)=><div className="rounded-lg bg-white/[.035] p-3" key={role}><p className="text-[10px] tracking-[.12em] text-white/30">{role.toUpperCase()}</p><p className="mt-2 flex items-center gap-2 text-xs"><Users size={12} className="text-violet-300"/>{j?`${Math.max(2,i+3)} members`:"View profile"}</p></div>)}</div>}</div>)}</div></div></section></PublicShell>}
