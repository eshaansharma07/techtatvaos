import Link from "next/link";
import { ArrowLeft, Calendar, Check, Clock, MapPin, Users } from "lucide-react";
import { PublicShell } from "@/components/public-shell";
import { events } from "@/lib/data";
export default async function EventDetail({params}:{params:Promise<{slug:string}>}){const {slug}=await params;const e=events.find(x=>x.slug===slug)||events[0];return <PublicShell><section className="mx-auto max-w-7xl px-6 pb-24 pt-32"><Link href="/events" className="flex items-center gap-2 text-xs text-white/45"><ArrowLeft size={14}/> BACK TO EVENTS</Link><div className={`relative mt-8 overflow-hidden rounded-3xl bg-gradient-to-br ${e.accent} p-8 md:p-14`}><div className="absolute inset-0 grid-bg opacity-25"/><div className="relative max-w-3xl"><span className="rounded-full bg-black/25 px-3 py-1.5 text-[10px] font-bold tracking-[.2em]">{e.type.toUpperCase()}</span><h1 className="mt-8 text-5xl font-medium tracking-[-.06em] md:text-8xl">{e.title}</h1><p className="mt-6 max-w-2xl text-sm leading-7 text-white/75">{e.desc}</p></div></div><div className="mt-6 grid gap-5 lg:grid-cols-[1fr_360px]"><div className="glass rounded-2xl p-7"><h2 className="text-xl">About the event</h2><p className="mt-4 text-sm leading-7 text-white/45">A focused, high-energy session for students who want to go beyond the lecture hall. Collaborate with a team, learn from experienced builders, and leave with something concrete.</p><h3 className="mt-9 text-sm">What to bring</h3><div className="mt-4 grid gap-3 sm:grid-cols-2">{["University ID card","Laptop and charger","Curiosity and focus","A problem worth solving"].map(x=><p className="flex items-center gap-2 text-xs text-white/55" key={x}><Check size={14} className="text-violet-300"/>{x}</p>)}</div></div><aside className="glass rounded-2xl p-6"><h3 className="text-sm">Event details</h3><div className="mt-5 space-y-4 text-xs text-white/50"><p className="flex gap-3"><Calendar size={15} className="text-violet-300"/>{e.date} 2026</p><p className="flex gap-3"><Clock size={15} className="text-violet-300"/>{e.time} onwards</p><p className="flex gap-3"><MapPin size={15} className="text-violet-300"/>{e.venue}</p><p className="flex gap-3"><Users size={15} className="text-violet-300"/>{e.registrations} / {e.capacity} seats claimed</p></div><button className="mt-7 w-full rounded-xl bg-white py-3.5 text-sm font-semibold text-black">Register for this event</button><p className="mt-3 text-center text-[10px] text-white/30">Confirmation and QR pass issued instantly</p></aside></div></section></PublicShell>}
