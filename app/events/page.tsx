import { Search, SlidersHorizontal } from "lucide-react";
import { EventCard } from "@/components/event-card";
import { PublicShell } from "@/components/public-shell";
import { events } from "@/lib/data";
export default function EventsPage(){return <PublicShell><section className="mx-auto max-w-7xl px-6 pb-24 pt-40"><p className="text-[10px] tracking-[.3em] text-violet-300">CLUB CALENDAR / 2026</p><h1 className="mt-5 text-6xl font-medium tracking-[-.07em] md:text-8xl">Enter the room.</h1><p className="mt-6 max-w-xl text-sm leading-7 text-white/45">Workshops, build nights, masterclasses, and showcases designed to move you from interested to involved.</p><div className="glass mt-12 flex flex-wrap items-center gap-3 rounded-2xl p-3"><div className="flex min-w-56 flex-1 items-center gap-3 px-3"><Search size={16} className="text-white/35"/><input className="w-full bg-transparent py-3 text-sm outline-none placeholder:text-white/25" placeholder="Search the calendar"/></div>{["ALL TEAMS","ALL FORMATS","UPCOMING"].map(x=><button key={x} className="rounded-xl border border-white/[.08] bg-white/[.04] px-4 py-3 text-[10px] tracking-[.14em] text-white/55">{x}</button>)}<button className="rounded-xl bg-violet-500 p-3"><SlidersHorizontal size={15}/></button></div><div className="mt-8 grid gap-4 md:grid-cols-2 lg:grid-cols-4">{events.concat(events.slice(0,2).map(e=>({...e,slug:`${e.slug}-archive`,status:"ARCHIVE",date:"MAY 26"}))).map(e=><EventCard event={e} key={e.slug}/>)}</div></section></PublicShell>}
